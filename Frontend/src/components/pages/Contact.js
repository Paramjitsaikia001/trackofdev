import MobileNav from "../navigation/mobilenav"
import { textStyles } from '../../utils/styles';

export default function contact() {
    return (
        <section>
            <div className={textStyles.body}>hello from contact</div>
            <MobileNav />
        </section>
    )
}